# OfferGuide 工作守则

## 产品目标

OfferGuide 只围绕一条完整求职链路工作：

1. **投递前**：主动从真实来源找岗位，按用户目标整理和排序，供用户选择。
2. **投递中**：根据用户已有简历和当前 JD 生成一份定制简历与投递包。简历内容由模型编辑，视觉格式由一套经过用户确认的模板稳定渲染；用户检查后自行提交。
3. **投递后**：基于实际提交的 JD 和简历准备公司信息、项目追问、技术题、学习材料和模拟面试。

产品完成度和实际可用性优先于成本、调用次数、测试数量、内部可观测性、所谓 agent 感或自进化指标。核心链路不好用时，不以新增外围功能代替修复。

用户手动粘贴 JD 是兜底，不是主要找岗方式。系统绝不替用户点击发送或提交。

## 不得擅自增加产品规则

- 不把示例、行业经验或某次调研结论升级成全局硬规则。
- 简历的内容取舍、展开程度和最终篇幅由当前 JD 与候选人的真实证据决定；排版层不得为了适配预设版面删减、缩小或挤压内容。
- 不把 ATS、成本、事实检查、测试覆盖率、提示词进化等辅助指标写成产品核心目标。
- 不因为内部数据结构存在，就强迫用户或 agent 维护 worldview、work item、reflection、fitness 等仪式性状态。
- 不同时生成多套互相竞争的简历。一个岗位只有一份当前定制简历；模板更新只重新排版，不重新调用模型改内容。
- 不用“规则通过”“测试通过”“坐标接近”代替真实效果。视觉任务必须查看最终页面或 PDF，并与用户指定参考逐页比较。

## 事实与调查

写代码前读取真实实现，不凭名字猜 schema、函数签名、Skill 输入输出、DOM、URL、配置或环境状态。

判定某物“不存在”前先枚举可见全集；关键词搜索的阴性结果不能单独作为不存在的证据。远程来源、平台行为和时效信息要真请求或使用可信原始来源验证。

不确定且无法验证的内容明确标为未知，不写假实现掩盖未知。

## 简历边界

- 学校、公司、岗位、时间、项目、奖项、数字结果等可外部核验的硬事实不得编造或篡改。
- 允许根据 JD 调整顺序、取舍、表达和强调重点，也允许加入与候选人已有能力相邻、面试前可准备到可解释程度的基础知识或工具声明。
- 新增能力声明不能伪装成已经发生的项目经历、业绩、上线规模或排名；需要准备的内容进入投后面试材料。
- 最终标准是正常、清楚、可信、适合当前岗位的真实简历，不是满足某个固定写作公式。

## 工程纪律

- 保留用户已有改动，不清理无关工作树。
- 修改范围以完成当前真实任务为准；删除旧系统前先确认调用关系。
- 测试用于防回归，不作为产品价值指标。按风险运行相关测试和完整回归。
- 不自动提交、推送或创建 PR；除非用户明确要求，不自动 commit。
- commit message 不加入模型署名。
