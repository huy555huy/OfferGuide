# OfferGuide 项目说明

这份文件只说明项目当前要解决的问题和真实主链路。具体简历内容必须在拿到目标 JD 后现场选择，不从项目说明反推一套固定写法。

## 一句话

OfferGuide 是一个中文求职 Agent：主动找到值得投的岗位，为选中的岗位生成一份基于用户真实经历的定制简历和投递包，并在实际投递后使用提交版本准备面试。

## 三个核心任务

### 投递前：找岗位

- 从多个真实来源发现岗位。
- 区分暑期实习、日常实习、校招和社招。
- 结合用户目标、地点、时间和经历做排序。
- 用户选择要推进的岗位。

### 投递中：做投递包

- 输入是用户主简历和当前岗位 JD。
- 模型负责内容选择、重排、改写和必要包装。
- 模板负责稳定生成正常可读的 PDF，不让模型用空格或手工换行控制视觉格式。
- 输出只有一份当前简历，同时准备当前岗位需要的投递话术、网申回答和提交前检查。
- 用户检查后自行提交；系统不替用户发送。

### 投递后：做面试准备

- 冻结实际提交的 JD 和简历。
- 根据这份材料准备公司与岗位信息、项目深挖、技术题、薄弱点学习和模拟面试。
- 面试后可以记录结果和复盘，但这不是投递前必须完成的仪式。

## 简历内容边界

- 学校、公司、岗位、日期、项目、比赛、奖项、数字结果等硬事实不能编造。
- 可以针对 JD 调整内容顺序、删减低价值信息、改写表达并突出更相关的经历。
- 可以加入与已有能力相邻、在面试前能够准备到可解释程度的知识或工具声明；这类声明不能被写成已经发生的项目结果。
- 需要补的知识进入当前岗位的面试准备材料，不套固定 5 天、7 天或固定题数。
- 最终取舍看它是否帮助招聘方理解候选人与当前岗位的关系，并且能在面试中解释和防守。

## 技术实现边界

当前用户路径由以下模块支撑：

- 找岗 Agent：`research_agents/job_discovery/`
- 共用 Agent runner、来源证据与后台协调：`research_agents/`
- 招聘平台适配器：`platforms/`
- 简历上下文：`resume/context.py`
- 简历内容：`resume/editor.py` 和唯一 `ResumeDocument`
- PDF 排版：`resume/render.py` 与唯一 Typst 模板
- 当前草稿和提交冻结：`resume/workspace.py`
- 完整投递编排：`resume/workflow.py`
- 投递材料：`apply_assistant` Skill 输出当前岗位实际需要的文案和回答
- 面试研究：`interview_research/` 绑定 submitted workspace 搜索来源并发布唯一当前材料
- 模拟面试：`mock_interview` Skill 读取同一份冻结投递和当前面试材料
- 页面：`ui/`

主对话 Agent、日志和运行诊断只是支撑能力。找岗、投递和面试研究的当前结果分别由明确的领域所有者生产，其他入口不能再维护一套评分、题单或结果副本。具体哪些技术值得写进某次求职简历，由目标 JD 和真实项目贡献决定，不在本文件预设。

## 面试介绍骨架

介绍项目时先讲真实用户问题和完整链路，再讲为解决具体难点采用的实现：

1. 求职者没有精力持续跨平台找岗位。
2. 通用简历不能充分对应每个 JD，且自动改写容易破坏格式。
3. 实际投递后，面试准备必须对应雇主收到的那份材料。
4. OfferGuide 把这三段连接成一个可恢复的申请流程，同时保留用户对提交动作的最终控制。

具体技术细节、项目取舍和简历表述在面对目标岗位时再选择，不在这里生成固定版本。
